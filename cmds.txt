Ubuntu 22.04
│
├── PHP 8.3
├── Composer
├── Nginx
├── PostgreSQL 16
├── Redis
├── Python 3.12
├── FastAPI
├── Git
├── VS Code Remote WSL
├── SSL for local domains
└── GitHub SSH authentication

wsl --install Ubuntu-22.04

code .

sudo apt update && sudo apt upgrade -y

sudo apt install php8.3 php8.3-cli php8.3-fpm php8.3-common php8.3-pgsql php8.3-mbstring php8.3-intl php8.3-curl php8.3-xml php8.3-zip php8.3-gd php8.3-bcmath php8.3-opcache git -y

sudo apt install composer -y

sudo vim /etc/nginx/sites-available/matrimony.conf

sudo ln -s /etc/nginx/sites-available/matrimony.conf /etc/nginx/sites-enabled/

sudo nginx -t

sudo systemctl restart nginx.service

curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo gpg --dearmor -o /usr/share/keyrings/postgresql.gpg

echo "deb [signed-by=/usr/share/keyrings/postgresql.gpg] http://apt.postgresql.org/pub/repos/apt jammy-pgdg main" | sudo tee /etc/apt/sources.list.d/pgdg.list

sudo apt update

sudo apt install postgresql-16 postgresql-client-16 postgresql-contrib -y

CREATE DATABASE matrimony_dev;
CREATE USER matrimony_dev_user WITH PASSWORD 'strong_dev_password';
GRANT ALL PRIVILEGES ON DATABASE matrimony_dev TO matrimony_dev_user;

sudo apt install redis-server -y
sudo systemctl enable redis-server
sudo systemctl start redis-server

Verify:

python --version
which python

Expected:

Python 3.12.x
/home/anant/venvs/webapp-matrimony/bin/python

cd /mnt/d/www/webapp-matrimony/backend

python3.12 -m venv ~/venvs/webapp-matrimony
source ~/venvs/webapp-matrimony/bin/activate

python -m pip install --upgrade pip setuptools wheel

Install the dependencies:

pip install fastapi "uvicorn[standard]" sqlalchemy alembic redis psycopg2-binary "python-jose[cryptography]" "passlib[bcrypt]" bcrypt pydantic-settings python-multipart httpx email-validator orjson python-dotenv loguru slowapi itsdangerous aiofiles jinja2

pip freeze > requirements.txt

Whenever you reopen WSL or VS Code, activate the environment using:

source ~/venvs/webapp-matrimony/bin/activate

To make activation easier, create a shortcut:

echo "alias matrimony-api='cd /mnt/d/www/webapp-matrimony/backend && source ~/venvs/webapp-matrimony/bin/activate'" >> ~/.bashrc
source ~/.bashrc

Later, simply run:

matrimony-api

